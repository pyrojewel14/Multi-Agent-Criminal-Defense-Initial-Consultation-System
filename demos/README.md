# 可复现 Demo 资产

本目录集中保存供演示和验收直接查看的输入契约与实际运行记录；可执行入口仍位于
`backend/examples/`，自动化断言仍位于 `backend/tests/`。

- `consultation/`：三条标准咨询 case 及字段契约。
- `rag/`：五条 RAG 查询、历史失败记录和 live 结果。

这些文件不是生产数据，也不替代 `evaluation/` 下的正式评估集。
